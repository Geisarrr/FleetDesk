import { Check } from "lucide-react";
const initials = (name) => name.split(" ").map((part) => part[0]).join("");
export default function ApprovalStep({ step, isLast }) { return <article className={`approval-step approval-step--${step.status}`}><div className="approval-step-rail"><span>{step.status === "completed" ? <Check size={15} /> : <i />}</span>{!isLast && <b />}</div><div className="approval-step-content"><div className="approval-step-heading"><div><h3>{step.title}</h3>{step.date && <p>{step.date} <i>•</i> {step.time}</p>}</div>{step.status === "current" && <em>Current stage</em>}</div><p className="approval-step-description">{step.description}</p>{step.user && <div className="approver-card"><span>{initials(step.user.name)}</span><div><strong>{step.user.name}</strong><small>{step.user.role}</small></div></div>}</div></article>; }
