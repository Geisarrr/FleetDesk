export default function ActivityPagination({ page, onPageChange }) { return <footer className="activity-pagination"><span>Page {page} of 3</span><nav><button type="button" onClick={() => onPageChange(Math.max(1, page - 1))}>Prev</button>{[1,2,3].map((number) => <button type="button" className={number === page ? "active" : ""} onClick={() => onPageChange(number)} key={number}>{number}</button>)}<button type="button" onClick={() => onPageChange(Math.min(3, page + 1))}>Next</button></nav></footer>; }
