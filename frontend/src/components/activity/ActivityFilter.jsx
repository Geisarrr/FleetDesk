const options = { dateRange: ["Today", "Last 7 Days", "Last 30 Days", "Custom"], userType: ["All Types", "User", "System Agent", "Security"], action: ["All Actions", "CREATE_BOOKING", "APPROVE_L1", "UPDATE_ROUTE", "AUTH_FAIL", "MAINT_FLAG"], entityType: ["All Entities", "Booking", "Vehicle", "User", "Route"] }; const labels = { dateRange: "Date Range", userType: "User Type", action: "Action", entityType: "Entity Type" };
export default function ActivityFilter({ open, filters, onChange }) { if (!open) return null; return <section className="activity-filter-popover">{Object.entries(options).map(([key, list]) => <label key={key}><span>{labels[key]}</span><select value={filters[key]} onChange={(event) => onChange({ ...filters, [key]: event.target.value })}>{list.map((item) => <option key={item}>{item}</option>)}</select></label>)}</section>; }
