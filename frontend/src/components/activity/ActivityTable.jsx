import { Bot } from "lucide-react";
import ActivityBadge from "./ActivityBadge";
export default function ActivityTable({ logs, onSelect }) { return <div className="activity-table-scroll"><table className="activity-table"><thead><tr><th>Timestamp</th><th>User / Agent</th><th>Action</th><th>Entity</th><th>Description</th></tr></thead><tbody>{logs.map((log) => <tr key={log.id} onClick={() => onSelect(log)} tabIndex="0" onKeyDown={(event) => event.key === "Enter" && onSelect(log)}><td>{log.timestamp}</td><td><div className="activity-user">{log.user.initial ? <span>{log.user.initial}</span> : <span className="system-user"><Bot size={15} /></span>}<div><strong>{log.user.name}</strong><small>{log.user.role}</small></div></div></td><td><ActivityBadge action={log.action} /></td><td className="activity-entity">{log.entity}</td><td className="activity-description">{log.description}</td></tr>)}{logs.length === 0 && <tr><td colSpan="5" className="activity-empty">No activities found for the active filters.</td></tr>}</tbody></table></div>; }
