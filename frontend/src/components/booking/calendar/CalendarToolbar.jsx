import { ChevronLeft, ChevronRight, Plus } from "lucide-react";
export default function CalendarToolbar({ view, onViewChange, onNewBooking, canCreateBooking }) { return <header className="calendar-toolbar"><div className="calendar-view-toggle"><button className={view === "week" ? "active" : ""} type="button" onClick={() => onViewChange("week")}>Week</button><button className={view === "month" ? "active" : ""} type="button" onClick={() => onViewChange("month")}>Month</button></div><div className="calendar-navigation"><button type="button" aria-label="Previous week"><ChevronLeft size={15} /></button><strong>Oct 24 - Oct 30, 2023</strong><button type="button" aria-label="Next week"><ChevronRight size={15} /></button></div>{canCreateBooking && <button className="calendar-new-booking" type="button" onClick={onNewBooking}><Plus size={14} /> New Booking</button>}</header>; }
