import BookingBar from "./BookingBar";
export default function VehicleRow({ vehicle, selectedDate, onDateSelect, onEventSelect }) { return <div className={`calendar-vehicle-row ${vehicle.status}`}><div className="calendar-vehicle-name"><strong>{vehicle.name}</strong><span><i />{vehicle.status}</span></div>{[24,25,26,27,28,29,30].map((day) => <button className={`calendar-day-cell ${selectedDate === `2023-10-${day}` ? "selected" : ""}`} type="button" key={day} onClick={() => onDateSelect(`2023-10-${day}`)} aria-label={`${vehicle.name}, October ${day}`} />)}{vehicle.events.map((event) => <BookingBar event={event} key={event.title} onClick={() => onEventSelect({ vehicle: vehicle.name, ...event })} />)}</div>; }
