import { Check, Clock3, MapPin, ShieldCheck, X } from "lucide-react";
import StatusBadge from "./StatusBadge";

const timeline = ["Created", "Manager Review", "Fleet Approval", "Completed"];
export default function BookingDetailDrawer({ booking, onClose }) {
  return <><button type="button" aria-label="Close booking details" className={`drawer-overlay ${booking ? "is-open" : ""}`} onClick={onClose} /><aside className={`booking-detail-drawer ${booking ? "is-open" : ""}`} aria-hidden={!booking}>{booking && <><header><div><p>Booking detail</p><h2>{booking.id}</h2></div><button type="button" onClick={onClose} aria-label="Close drawer"><X size={18} /></button></header><div className="drawer-status"><StatusBadge status={booking.status} /><span>{booking.approval}</span></div><section className="drawer-information"><h3>Request details</h3><dl><div><dt>Requester</dt><dd>{booking.requester}</dd></div><div><dt>Department</dt><dd>{booking.department}</dd></div><div><dt>Vehicle</dt><dd>{booking.vehicle}</dd></div><div><dt>Driver</dt><dd>{booking.driver}</dd></div><div><dt><MapPin size={12} /> Destination</dt><dd>{booking.destination}</dd></div><div><dt><Clock3 size={12} /> Schedule</dt><dd>{booking.date}</dd></div></dl></section><section className="approval-timeline"><h3>Approval timeline</h3>{timeline.map((step, index) => <div className={index < 2 ? "done" : index === 2 && booking.status === "Approved" ? "done" : ""} key={step}><span>{index < 2 ? <Check size={12} /> : <ShieldCheck size={12} />}</span><p>{step}<small>{index === 0 ? "Request initialized" : index === 1 ? booking.approval : "Awaiting workflow"}</small></p></div>)}</section></>}</aside></>;
}
