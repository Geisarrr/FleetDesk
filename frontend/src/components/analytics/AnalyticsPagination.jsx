export default function AnalyticsPagination({ page, onPageChange }) { return <footer className="analytics-pagination"><span>Page {page} of 3</span><nav><button type="button" onClick={() => onPageChange(Math.max(1, page - 1))}>Prev</button>{[1,2,3].map((number) => <button type="button" className={page === number ? "active" : ""} onClick={() => onPageChange(number)} key={number}>{number}</button>)}<button type="button" onClick={() => onPageChange(Math.min(3, page + 1))}>Next</button></nav></footer>; }
