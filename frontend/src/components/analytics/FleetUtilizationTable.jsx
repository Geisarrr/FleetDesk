const tone = { Completed: "completed", Approved: "approved", Pending: "pending", Rejected: "rejected", Cancelled: "cancelled" };
export default function FleetUtilizationTable({ rows, onSelect }) { return <div className="analytics-table-scroll"><table className="analytics-table"><thead><tr><th>Booking ID</th><th>Date</th><th>Requester</th><th>Vehicle</th><th>Driver</th><th>Destination</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id} onClick={() => onSelect(row)} tabIndex="0" onKeyDown={(event) => event.key === "Enter" && onSelect(row)}><td className="analytics-booking-id">{row.id}</td><td>{row.date}</td><td>{row.requester}</td><td>{row.vehicle}</td><td>{row.driver}</td><td>{row.destination}</td><td><span className={`analytics-status ${tone[row.status]}`}><i />{row.status}</span></td></tr>)}{rows.length === 0 && <tr><td className="analytics-empty" colSpan="7">No report entries match these parameters.</td></tr>}</tbody></table></div>; }
