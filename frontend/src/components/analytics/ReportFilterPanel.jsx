import { Search } from "lucide-react";
const options = { dateRange: ["Today", "Last 7 Days", "Last 30 Days", "Custom Range"], region: ["All Regions", "North", "South", "East", "West"], site: ["All Sites", "Metro HQ", "Airport Hub", "Regional HQ", "Client Site", "North Depot"], vehicle: ["All Vehicles", "Tesla Model 3", "Ford Transit Custom", "Chevy Bolt EV", "Toyota Prius", "Toyota Hilux"], driver: ["All Drivers", "Self Drive", "John Doe", "Kevin Moss"], status: ["All Statuses", "Pending", "Approved", "Rejected", "Completed", "Cancelled"] };
const labels = { dateRange: "Date Range", region: "Region", site: "Site", vehicle: "Vehicle", driver: "Driver", status: "Status" };
export default function ReportFilterPanel({ filters, onChange }) { const update = (key, value) => onChange({ ...filters, [key]: value }); return <section className="report-filter-panel"><header><div><p>Report parameters</p><h2>Refine operational data</h2></div><label className="analytics-search"><Search size={15} /><input value={filters.search} onChange={(event) => update("search", event.target.value)} placeholder="Search analytics..." /></label></header><div className="analytics-filter-grid">{Object.keys(options).map((key) => <label key={key}><span>{labels[key]}</span><select value={filters[key]} onChange={(event) => update(key, event.target.value)}>{options[key].map((option) => <option key={option}>{option}</option>)}</select></label>)}</div></section>; }
